``` 
  const quantidadeElementos = arrayDeNumeros.filter(x => x === numeroEscolhido).length
  if (quantidadeElementos) {
return `O número ${numeroEscolhido} aparece ${quantidadeElementos}x`}
  else { return 'Número não encontrado' }   
}

```
