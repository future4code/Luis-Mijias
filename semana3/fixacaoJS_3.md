 ```

 const media = (ex*1 + p1*2 + p2 * 3) / 6
    let nota 
    if (media >= 9){ nota = "A"
  }if (media < 9 && media >= 7.5){ nota="B"
  }if (media < 7.5 && media >= 6){ nota = "C"
  }if (media < 6){ nota = "D"
  }
 return nota
  }
  
  ````